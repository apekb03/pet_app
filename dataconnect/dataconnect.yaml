specVersion: "v1"
serviceId: "petapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "petapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
